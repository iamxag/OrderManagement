@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<partial name="_Carousel" />

<h1>Offers for week</h1>

@foreach(Product product in Model.Products)
{
    <partial name="_ProductCard" model="product"/>
}